import time
import random

def unique_code():
    return f"{int(time.time())}{random.randint(1000,9999)}"

print(unique_code())
import secrets import string 
def generate_long_code(length=128): 
characters = string.ascii_letters + string.digits + string.punctuation return ''.join(secrets.choice(characters) 
for _ in range(length)) 
print(generate_long_code())
